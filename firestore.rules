rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Public site content — anyone may read
    match /content/{docId} {
      allow read: if true;
      // Only authenticated members may edit
      allow write: if request.auth != null;
    }
  }
}